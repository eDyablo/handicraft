import org.junit.*
import static org.hamcrest.MatcherAssert.assertThat
import static org.hamcrest.Matchers.*

class IntegrationPipelineTest {
  @Test void run() {
    def pipeline = new IntegrationPipeline()
  }
}
