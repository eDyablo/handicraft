#include <string>

namespace coin {
  namespace mzn {
    auto longest_string(size_t a, size_t b, size_t c) {
      using namespace ::std;
      return string("");
    }
  }  // namespace mzn
}  // namespace coin
